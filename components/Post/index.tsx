import { FC } from 'react';
import styles from './index.module.scss';

type PropsType = {
  className: string;
};

const Post: FC<PropsType> = ({ className }) => {
  return <div>index</div>;
};

export default Post;
